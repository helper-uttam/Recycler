import classes from "./Footer.module.css";

const Footer = () => {

    return <div className={classes.footer}>
    <p className={classes.content}>thankyou for using!</p>
    </div>
}

export default Footer;